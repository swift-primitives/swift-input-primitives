# swift-input

Dissolved. This package is a tombstone.

- `Input.Stream.Protocol` is `Iterator.Protocol` in swift-atoms/swift-iterator.
- `Input.Restorable` is `Restorable` in swift-atoms/swift-checkpoint.
- `Input.Protocol` is `Cursor.Protocol` in swift-atoms/swift-cursor; standard-library slices conform through its standard-library integration.
- `Input.Slice` and `Input.Buffer` are `ArraySlice` and `Array`.
